class CityResponse {
  constructor({ id, enCode, cityName, areaCode }) {
    this.id = id;
    this.enCode = enCode;
    this.cityName = cityName;
    this.areaCode = areaCode;
  }
}

export default CityResponse;
