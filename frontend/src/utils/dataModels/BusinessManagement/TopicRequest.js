class TopicRequest {
  constructor({
    id,
    researchTopic,
    effect,
  }) {
    this.id = id;
    this.research_topic = researchTopic;
    this.effect = effect;
  }
}

export default TopicRequest;
